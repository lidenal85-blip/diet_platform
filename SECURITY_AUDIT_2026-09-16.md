# 🔐 SECURITY AUDIT: инцидент компрометации токена @Fatboyandgirl_bot

> Дата: 2026-09-16 · Исполнитель: Buffy / Freebuff
> Триггер: владелец сообщил о взломе бота; далее найдено продолжение — секреты
> 2,5 месяца лежали в ПУБЛИЧНОМ GitHub-репо (`.env.bak-20260630181938`).
> Все факты проверены командами; отпечатки секретов маскированы.

---

## 0. TL;DR

Утечка локализована: **единственный секретоносец за всю историю —
`.env.bak-20260630181938`** (первый коммит `15c35be`, 2026-07-01, публичный
репо `lidenal85-blip/diet_platform`). Внутри: 9 ключей Gemini, токен бота,
relay-токен. **Сервер не скомпрометирован**, бот не угнан (имя/описания/webhook
аутентичны). Токен бота ротирован, прод работает на новом; репо переведён в
Private; утёкший файл снят с трекинга; развёрнут 5-минутный монитор
идентичности с алертом.

**Что осталось:** R3 (удалить 9 Gemini-ключей), R4 (ротация PAT из remote URL),
R6 (чистка истории + force-push), финальная ротация токена из чата, 2FA/сессии
Telegram-аккаунта, GitHub Secret Scanning (только в видимости GitHub), Gitleaks.

---

## 1. §1 Инцидент-реакция (выполнено)

| Пункт плана | Результат |
|---|---|
| Revoke токена (BotFather) | ✅ Владелец; **OLD → HTTP 401** на все методы (проверено с сервера) |
| Идентичность: getMe/getMyName/description | ✅ Аутентичны: `@Fatboyandgirl_bot` / «ПУХЛЯШ» / «😻» / «ВКУСНО И С ПОЛЬЗОЙ 😋» — **не подменялись** |
| getWebhookInfo старого токена | ✅ Новому токену: url пуст, 0 pending — **левого webhook нет**; старому — 401 (проверка невозможна и не нужна: токен мёртв) |
| Восстановление name/desc через BotFather | ✅ Не потребовалось — ничего не подменено |

**Прод-перевод:** `.env` на whimco обновлён (stdin, бэкап `.env.bak-rot-20260916-065751`,
chmod 600), рестарт чистый (RC=0, без зомби-этапа), новый PID 1832232, health ok,
0 Traceback. Серв. verify: md5 в проде = `25c4e5c117` ≠ утёкшего `00f93beab1`.

## 2. §2 Источник утечки (найден и локализован)

| Срез | Результат |
|---|---|
| `git log --all --diff-filter=A` + grep по полной истории | **Единственный секретоносец — `.env.bak-20260630181938`** (трекается с `15c35be` 2026-07-01) |
| Токен-паттерны в полной истории | Ровно один bot-id `8954955644` + 9 AIza-префиксов — совпадают с содержимым `.env.bak` один-в-один |
| PAT в git-истории | **0 вхождений** — PAT жил только в `.git/config` (remote URL), в историю не попадал |
| `.gitignore` | `.env` игнорировался, **`.env.bak*` — нет** → файл прошёл мимо |
| Права на сервере | `.env` и бэкап ротации: **600 root** ✓ |
| Логи | 0 вхождений токен-паттерна в journald за сентябрь ✓ |
| Трекинг сейчас | Снят с трекинга сегодня (R5, коммит этого отчёта); `.env.*` в `.gitignore` |
| Другие репо / бэкапы / docker | `diet_platform` не трекается в workstation-репо (локально и на сервере); docker на сервере нет; токен-паттернов в env/bak-файлах /opt и /root нет |
| Workstation-репо `Denis-kaa/freebuff` | ⚠️ **Публичный**; токен бота в трекаемых файлах не встречается, `.env` не трекается. Если история когда-либо содержала секреты — Gitleaks (§5). Требует **решения по видимости** |

**Вывод:** путь утечки — публичный GitHub. Иных механизмов не найдено.

## 3. §3 Компрометация сервера (чисто)

| Проверка | Результат |
|---|---|
| `last` (wtmp с 2026-08-24) | **Все** SSH-сессии — с одного IP владельца `2.56.212.97`; посторонних нет |
| `who` | root tty1 + tmux — обычная среда владельца |
| Кроны (все юзеры) | Ровно 2 job: `freebuff-autodeploy` (*/5) и `nightly_audit` — оба помечены, владелец подтверждает |
| `authorized_keys` | Единственный ключ `ssh-ed25519 termux@freebuff` (телефон владельца) |
| Процессы | Инфраструктура владельца (freebuff, cline, ai-dubber, warp-svc, postgres…) + diet-platform; аномальных miners/tunnels нет |
| Сессии Telethon/Pyrogram (*.session) | **Не найдено** нигде (/opt, /root, /home) |
| Порты | Только известные сервисы (8000/8010/8020–8022, postgres на localhost, 62789 localhost xray-клиент); diet-platform слушает локально |

## 4. §4 Долгосрочная защита (развёрнуто)

| Мера | Статус |
|---|---|
| Токен только через env | ✓ Было так; утечка была не в коде, а в бэкап-файле в git |
| `.gitignore` покрывает `.env.*` | ✓ Добавлено сегодня (R5) |
| Pre-commit заслон от секретов | 🔲 **TODO (предлагаю в Phase 1):** `detect-secrets`/`gitleaks` hook + CI-джоба, грепающий `AIza|ghp_|[0-9]{8,12}:AA` |
| Ротация после инцидента | ✅ Выполнена (сейчас) |
| Регулярная ротация | 🔲 Кандидат: календарная задача (например, раз в квартал) |
| 2FA Telegram-аккаунта + Settings → Devices | 🔲 **Ручной шаг владельца** — особенно важно: у угнанного аккаунта-владельца BotFather отбирают бота целиком |
| Мониторинг идентичности | ✅ **Развёрнут:** `/opt/diet_platform/scripts/identity_monitor.py` + `identity_baseline.json` + systemd `diet-identity-monitor.timer` (5 мин, oneshot). Сверяет getMe/getMyName/getMyDescription/webhook с baseline; алерт в journald + DM владельцу при заданном `TELEGRAM_ADMIN_CHAT_ID`; дедуп через `/run`-state; прогон подтверждён (`identity OK`) |
| DM-канал | 🔲 Владельцу: задать `TELEGRAM_ADMIN_CHAT_ID=<свой chat_id>` в проде `.env` — тогда алерты будут приходить в Telegram |

## 5. §5 Остаточные действия

**R3 — 9 ключей Gemini: ✅ ВЫПОЛНЕНО И ВЕРИФИЦИРОВАНО 2026-09-16.** Ключи удалены
владельцем в AI Studio. Контрольный E2E с реальным LLM на whimco: **PASS exit 0**
(карточки 2.9s, план 4.5s — реальные вызовы API; KeyPool: gemini 3 + groq 3;
§20-8 инварианты целы; прод-пул `AIzaSyB5...omeQ` не задет, прод-конфиг не менялся).

**R4 — GitHub PAT (`ghp_…` в remote URL):** revoke в Settings → Developer
settings → выпустить fine-grained (только diet_platform/contents) →
`git remote set-url origin https://github.com/lidenal85-blip/diet_platform.git`
(затем credential при первом push). Учесть: этот же PAT лежит в `.git/config`
**телефона** и имел доступ к `Denis-kaa/freebuff` — если давал доступ к обоим
репо, ротация закрывает и этот вектор.

**R6 — чистка истории (после R3/R4):** `git filter-repo --invert-paths --path
.env.bak-20260630181938 --force` → гейт `git log --all -- .env.bak*` пуст →
пере-добавить origin → force-push (клонов нет; сервер не git-клон). После —
GitHub Support: GC unreachable-объектов. Альтернатива — recreate репо.

**Финальная ротация токена (30 сек):** токен из этой сессии засветился в чате
→ после R3/R4 владельцу: BotFather → Revoke → новый токен → сообщить любым
безопасным каналом (можно прямо в чат — я вставлю на сервер stdin-методом и
сотру). Текущий уже отрезан от публичной экспозиции (репо Private + история
будет переписана), но гигиена — прежде всего.

**Telegram-аккаунт:** включить облачный пароль (2FA), Settings → Devices →
убить незнакомые сессии.

**GitHub Secret Scanning:** для приватного репо — только в видимости GitHub
(если придёт уведомление о ключах, они уже в очереди на отзыв поставщиком).
Активно проверять — Gitleaks локально (Phase 1).

**Private-видимость workstation-репо `Denis-kaa/freebuff` (решение владельца):**
в трекаемых файлах чисто; но это живой публичный репо всей платформы — я бы
перевёл в Private.

## 6. Ручные шаги владельца (сводка)

1. **R3:** удалить 9 Gemini-ключей в AI Studio.
2. **R4:** revoke PAT; `git remote set-url` (команда в §5).
3. **R6:** сказать «делай R6» — выполню (bundle-бэкап → filter-repo → гейты → force-push).
4. **2FA + Devices** в Telegram.
5. `TELEGRAM_ADMIN_CHAT_ID` в прод `.env` — включить DM-алерты монитора.
6. (Опционально) `Denis-kaa/freebuff` → Private.
7. (Опционально, Phase 1) pre-commit gitleaks/detect-secrets.
