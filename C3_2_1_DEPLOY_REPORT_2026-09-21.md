# ОТЧЁТ C-3.2.1 — ДЕПЛОЙ НА ТЕСТОВЫЙ ПРОД WHIMCO

> Дата: 2026-09-21 · Основание: явное разрешение владельца на деплой C-3.2.1
> Границы: только `bot_handlers/today.py` + `tests/test_today.py`; без commit/push; без C-3.3

---

## 1. ИТОГ

**SUCCESS** — деплой выполнен, все обязательные проверки пройдены, откат не требовался.

## 2. ДОСТАВЛЕННЫЕ ФАЙЛЫ

| Файл | Действие |
|---|---|
| `/opt/diet_platform/bot_handlers/today.py` | перезаписан версией C-3.2.1 (scp) |
| `/opt/diet_platform/tests/test_today.py` | перезаписан версией C-3.2.1 (scp) |

Другие файлы не перезаписывались. Сервер до деплоя содержал ровно версии C-3.2 (md5 подтверждены) — уникальных серверных правок не было, конфликтов не возникло.

## 3. КОНТРОЛЬНЫЕ СУММЫ

| Файл | Локально (C-3.2.1) | Сервер до | Сервер после |
|---|---|---|---|
| `bot_handlers/today.py` | `f4693c33102815c0419f620a2037601b` | `7d46153fca199f4fad467e26cdc943fa` | `f4693c33102815c0419f620a2037601b` ✅ |
| `tests/test_today.py` | `4fcf4abda0977f92901ca99818f5ffe8` | `04d58ff417e6015bf831b92f22e8d487` | `4fcf4abda0977f92901ca99818f5ffe8` ✅ |

## 4. РЕЗЕРВНЫЕ КОПИИ

- `/root/dp_backups/20260921_pre_c321/bot_handlers/today.py` — md5 `7d4615…` (= сервер до), 16 713 байт
- `/root/dp_backups/20260921_pre_c321/tests/test_today.py` — md5 `04d58f…` (= сервер до), 17 857 байт
- Каталог `/root/dp_backups/20260921_pre_c321/`, права `go-rwx` (600), владельцем прочитаны, верифицированы по md5; скан на секреты (BOT_TOKEN/api_key/ghp_/sk-): **0 совпадений**. БД в рамках C-3.2.1 не изменяется — бэкап БД для этого этапа не требовался (последний валидный: `/opt/diet_platform/backups/` от деплоя C-3.2, 2026-09-20).

**Команда отката** (не выполнялась):
```bash
cp /root/dp_backups/20260921_pre_c321/bot_handlers/today.py /opt/diet_platform/bot_handlers/today.py
cp /root/dp_backups/20260921_pre_c321/tests/test_today.py /opt/diet_platform/tests/test_today.py
systemctl restart diet-platform.service
```

## 5. ПЕРЕЗАПУСК И HEALTH-CHECK

| Проверка | Результат |
|---|---|
| Предрестарт-верификация | py_compile OK; импорт-смоук в прод-venv: `meal_kb` даёт 2 ряда действий с корректными callback, `today_meal_info` на месте |
| `systemctl restart` | **exit 0** |
| MainPID | 3076984 → **3181833**, `active/running`, NRestarts=0 |
| Дубли процессов | нет — ровно один процесс бота (второе совпадение pgrep — сама ssh-команда, ложное) |
| `/health` (127.0.0.1:8150) | **200 OK** |
| Журнал запуска | bootstrap → Telegram bot starting → Database initialized → Uvicorn on :8150 → Pipeline worker started; поллинг активен |
| Ошибки | 0 ошибок импорта/синтаксиса/приложения. Записи `State 'stop-sigterm' timed out` + `Failed with result 'timeout'` — известная зомби-фаза **остановки старого** процесса (systemd убил его по TimeoutStopSec), к новому коду отношения не имеет; рестарт завершился штатно |

## 6. ТЕСТЫ

| Проверка | Результат |
|---|---|
| Профильные `pytest tests/test_today.py` (локально, 18 тестов, incl. 2 новых C-3.2.1) | **18 passed** |
| Полная регрессия (локально) | 107 passed, 10 failed — все 10 предсуществующие vault-фейлы (`vault_client_v2` не существует ни локально, ни на сервере) |
| Структурная проверка на **реальных данных прода** (read-only `mode=ro`) | 4 приёма за сегодня → 13 рядов клавиатуры: 4 блока «заголовок + 2 ряда действий»; id заголовков = id записей meal_state; **все** action-callback'и ссылаются на реальные meal_state |
| pytest в прод-venv | недоступен (модуль не установлен; установка в прод-окружение запрещена ТЗ) — компенсировано компиляцией, импорт-смоуком и read-only структурной проверкой на живых данных |

Пример фактического вида с прода:

```
🌅 Завтрак · Омлет из двух яиц с томатами
✅ Съел | ⏭ Пропустил
🔁 Заменить | 📖 Рецепт
🍽 Обед · Индейка с бурым рисом и струч…
✅ Съел | ⏭ Пропустил
🔁 Заменить | 📖 Рецепт
…
```

## 7. СХЕМА БД

**Не изменялась.** C-3.2.1 не содержит DDL (проверено грепом diff до деплоя); миграции не запускались; БД открывалась только read-only (`mode=ro`).

## 8. COMMIT / PUSH

**Не выполнялись.** Локальный репозиторий по-прежнему HEAD `a28b0f2` с рабочим деревом C-3.0–C-3.2.1 (8 modified + untracked). Удалённые Git-операции не производились.

## 9. ИЗВЕСТНЫЕ ОГРАНИЧЕНИЯ

1. Зомби-фаза остановки сервиса сохраняется (старый процесс не реагирует на SIGTERM ~90 с) — на результат деплоя не повлияла, но требует отдельного решения (например, graceful shutdown / `TimeoutStopSec` + `KillMode=mixed`).
2. pytest в прод-venv отсутствует — серверные тесты прогоняются только локально + read-only проверки на копии/проду.
3. Предсуществующие vault-фейлы (10) непроверяемы в обоих окружениях.
4. Хвосты прежних этапов (не входят в C-3.2.1): `RECIPES_SCHEMA` не исполняется `init_db()`; хендлер «Сегодня» без `StateFilter`.

## 10. ИНСТРУКЦИЯ ВЛАДЕЛЬЦУ — ЖИВАЯ ПРОВЕРКА В TELEGRAM

Бот уже работает на новой версии — ничего перезапускать не нужно.

1. Открой чат с ботом → **/start** (или «🔄 Обновить» в меню) — в главном меню есть кнопка **«📅 Сегодня»**.
2. Нажми **«📅 Сегодня»** (или отправь `/today`).
3. **Что должно быть видно:** каждое блюдо — отдельным блоком: строка «🍳 Завтрак · …» и сразу под ней её две пары кнопок `[✅ Съел] [⏭ Пропустил]` и `[🔁 Заменить] [📖 Рецепт]`; в конце ряда «🔄 Обновить». У тебя сегодня 4 приёма (завтрак/обед/ужин/перекус).
4. Нажми **заголовок блока** (например «🌅 Завтрак · Омлет…») → всплывёт алерт с полным названием блюда и статусом «запланировано». Экран не изменится.
5. Нажми **«✅ Съел»** у завтрака → заголовок блока станет «🍳 Завтрак ✅ · …», внизу «✅ Съедено: 1 из 4».
6. Нажми **«✅ Съел»** ещё раз у того же блюда → алерт «Уже отмечено как…», изменений и второго события нет.
7. Нажми **«⏭ Пропустил»** у другого блюда → его заголовок получит значок ⏭, счётчики обновятся.
8. Нажми **«📖 Рецепт»** у блюда → покажет сохранённый рецепт, если он есть; иначе честное «пока не подключён» (генерации нет).
9. Нажми **«🔁 Заменить»** → честное «функция будет доступна позднее», ничего не меняется.
10. Нажми **«🔄 Обновить»** → экран перерисуется с актуальными статусами.

**Проверка изоляции данных:** кнопки чужих записей (id из другого чата) обрабатываются отказом «⚠️ Запись не найдена» — покрыто тестами; вживую имитировать не нужно.

**Если что-то не так:** сообщи, откат на C-3.2 — одна команда из §4 (копия + рестарт).

## 11. ОТКАТ

**Не выполнялся** — не потребовался. Точка отката зафиксирована в §4.

---

**C-3.2.1 DEPLOYED — ОЖИДАНИЕ ЖИВОЙ ПРОВЕРКИ ВЛАДЕЛЬЦА. C-3.3 не начинал, commit/push не выполнялись.**
