# ОТЧЁТ C-3.0–C-3.2: ДЕПЛОЙ И E2E-ПРОВЕРКА

> Дата: 2026-09-20 · Сервер: whimco (`/opt/diet_platform`) · Сервис: `diet-platform.service`
> Объём: только C-3.0 (событие `meal_status_changed`), C-3.1 (единый модуль клавиатур), C-3.2 (экран «Сегодня»).

---

## 1. Итоговый статус

**SUCCESS WITH LIMITATIONS** — деплой выполнен, E2E-набор 35/35 PASS на изолированной копии прод-БД, сервис работает на новом коде. Ограничения: 10 vault-тестов непроверяемы в принципе (несуществующий модуль в обоих окружениях), проверки живым Telegram-клиентом не выполнялись (проверка — программная, на копии БД).

## 2. Исходное состояние

| Параметр | Факт |
|---|---|
| Источник кода | локальный репо `projects_17/diet_platform`, ветка `master`, HEAD `a28b0f2` + незакоммиченные C-3.0/C-3.1/C-3.2 (коммит вне объёма задачи) |
| Сервер | `/opt/diet_platform` — **не** git-репозиторий; 9 серверных файлов md5-идентичны локальному HEAD → уникальных серверных правок **нет**, перезапись безопасна |
| Сервис | `diet-platform.service` → `venv/bin/python main.py`, health :8150 |
| БД | `/opt/diet_platform/diet_platform.db`, WAL, integrity ok: 2 пользователя, 2 плана (1 active 20–26.09 + 1 archived), 56 meal_state (все `planned`), 12 событий, 85 рецептов |

## 3. Проверка изменений

| Файл/модуль | Изменение | Соответствует объёму | Результат |
|---|---|---|---|
| `planner/events.py` | `meal_status_changed` в `PHASE0_EVENTS` (7→8) + докстринг | C-3.0 | ✅ |
| `bot_handlers/keyboards.py` | **новый** — SoT клавиатур (2 варианта меню как есть) + кнопка «📅 Сегодня» в оба | C-3.1 + C-3.2 (минимально) | ✅ |
| `bot_handlers/today.py` | **новый** — экран «Сегодня», `ms:{id}:eat|skip|replace|recipe`, `/today` | C-3.2 | ✅ |
| `bot.py` | регистрация `today_router` (до FSM-роутеров), импорт клавиатур | C-3.1/C-3.2 | ✅ |
| `recipes/cabinet/diet_picker/meal_schedule_v2/puhlyash_settings/schedule.py` | compat-shim на `keyboards.py` (имена/сигнатуры сохранены) | C-3.1 | ✅ |
| `tests/test_today.py` | **новый** — 16 тестов | C-3.2 | ✅ |

Посторонних изменений нет; секретов в доставляемых файлах нет (grep-скан); DDL в diff отсутствует.

## 4. Резервные копии

- **БД:** `/opt/diet_platform/backups/dp_prod_backup_20260920_*.db` (sqlite3 `.backup` онлайн, без остановки; `PRAGMA integrity_check` ok; сверка по 6 таблицам с продом — идентична).
- **Код:** `/opt/diet_platform/backups/dp_code_pre_c3_20260920.tar.gz` — 135 файлов; первый вариант архива содержал 3 файла `.env.bak-rot-*` с остатками старых секретов → архив **пересоздан** с исключением, grep-скан финала чист.
- **Восстановление:** `systemctl stop diet-platform.service` → `cp <бэкап>.db diet_platform.db` → `tar xzf <бэкап>.tar.gz -C /opt/diet_platform` → `systemctl start ...`.

## 5. Результат деплоя

- Доставлены 10 файлов (tar по ssh), md5 на сервере = md5 локальных.
- `py_compile` + импорт-смоук всех изменённых модулей в прод-venv — OK.
- Рестарт: воспроизвалась известная **зомби-фаза** остановки (`stop-sigterm`, процесс старого кода не умирал ~90 с и продолжал отвечать /health); systemd прибил его по TimeoutStopSec (SIGKILL) и поднял новый.
- После рестарта: `ActiveState=active`, `MainPID=3076984` (старт 13:17:15 UTC), **NRestarts=0** (значит импорты нового `bot.py`/`today.py`/`keyboards.py` успешны — иначе был бы crash-loop), `/health` → `{"status":"ok",...}` 200, процессов бота **один**, ошибок в журнале нового старта нет.

## 6. Результаты тестирования

### Автоматические (локально)
| Проверка | Результат | Примечание |
|---|---|---|
| `tests/test_today.py` | **16/16 PASS** | все 13 пунктов ТЗ C-3.2 |
| Полная регрессия | **105 passed, 10 failed** | все 10 — `test_vault_integration.py` (см. §7-2), файл не менялся с baseline |

### Интеграционные (vault)
| Проверка | Результат |
|---|---|
| vault-тесты на сервере | **НЕПРОВЕРЕНЫ** — `pytest` отсутствует в прод-venv, а модуль `vault_client_v2` не существует ни на сервере, ни локально (см. §7-2) |

### E2E на сервере — изолированная копия прод-БД (`/tmp/dp_e2e_copy.db`)
**35 PASS / 0 FAIL.** Ключевое:

| Проверка | Результат | Доказательство |
|---|---|---|
| E1: кнопка «📅 Сегодня» в меню (оба варианта) | ✅ | canonical + legacy |
| E2: `/today` показывает блюда | ✅ | 4 приёма, названия из meal_state |
| E2: canonical chain (PATCH-2) | ✅ | экран взял 4 записи active-плана из 8 «сырых» — archived отфильтрован |
| E2: пустое состояние без генерации | ✅ | |
| E2: клавиатура 2 ряда/приём + «🔄 Обновить» | ✅ | rows=9 |
| E2: callback-формат `ms:{id}:eat|skip|replace|recipe` | ✅ | |
| E2: безопасный callback (битый формат / неизвестный id / None) | ✅ | 5/5, без исключений |
| E3: eaten → БД + событие `meal_status_changed` (+1) | ✅ | в копии |
| E3: props без health_notes/текстов блюд | ✅ | |
| E3: повторное нажатие — идемпотентно, событие не дублируется | ✅ | 1→1 |
| E3: запрет перехода eaten→skip, без события | ✅ | 2→2 |
| E3: чужая запись — отказ + не изменилась | ✅ | |
| E2: refresh — счётчик «Съедено: 1 из 4», ✅ у съеденного | ✅ | |
| E5: replace — заглушка «позже», БД не меняется | ✅ | |
| E5: recipe без привязки — честный ответ | ✅ | |
| E5: recipe по привязке — показан сохранённый рецепт | ✅ | «📖 Лимонно-сливочные спагетти…» |
| E5: recipe не создаёт рецепты | ✅ | n=85 → 85 |

### Фактические проверки в Telegram
**НЕ ВЫПОЛНЯЛИСЬ** (живой клиент) — перечислены в §9 как непроверенные.

## 7. Обнаруженные проблемы

1. **Зомби-фаза остановки сервиса** (известная, воспроизвелась): при `restart` старый процесс в `stop-sigterm` живёт до TimeoutStopSec (~90 с), продолжая отвечать. Влияние: долгий рестарт, окно «старый код отвечает». Временного решения в объёме C-3 нет; лечится настройкой юнита (KillMode/TimeoutStopSec) — отдельной задачей.
2. **`test_vault_integration.py` импортирует несуществующий `vault_client_v2`** — 10 тестов красные в любом окружении, непроверяемы в принципе. Воспроизведение: любой pytest-прогон. Влияние: шум в регрессии. Вне объёма — рекомендация: удалить тест или восстановить модуль.
3. **Инцидент в процессе E2E (устранён):** промежуточная версия E2E-скрипта (v2) потеряла патч `DB_PATH` и записала 5 тестовых событий + 2 изменения статуса **в прод-БД**. Обнаружено по контрольной сверке; выполнен **точечный откат по бэкапу** с верификацией: прод = 56 planned / 12 events / 0 meal_status_changed / 85 recipes. Финальный v3-скрипт работает строго на копии; после прогона прод повторно сверен — чист.

## 8. Откат

- **Доступен:** бэкапы §4 (БД + код), точка = состояние до деплоя (9 серверных файлов = HEAD `a28b0f2`).
- **Фактически выполнялся:** только точечный откат тестовых артефактов из прод-БД (§7-3) — успешен, верифицирован.
- Откат кода/рестарта **не требовался** — деплой прошёл чисто.

## 9. Что осталось неизвестным

1. Поведение кнопок/экрана **живым Telegram-клиентом** не проверялось (требуется владелец: открыть «📅 Сегодня», отметить блюдо).
2. Факт активного Telegram-поллинга подтверждён косвенно (процесс жив, NRestarts=0, health ok) — входящие апдейты не зондировались.
3. Vault-тесты — непроверяемы (§7-2).
4. Логи приложения в journal почти пусты (мало пишет в stdout) — поведение при ошибке поллинга не наблюдаемо через журнал.

## 10. Рекомендация следующего шага

1. Живая проверка владельцем в Telegram: «📅 Сегодня» → отметить блюдо → проверить счётчик и «🔄 Обновить».
2. Коммит рабочего дерева (C-3.0 + C-3.1 + C-3.2 + отчёты) — по отдельной авторизации.
3. Отдельные задачи (вне C-3): лечить зомби-остановку юнита; разобраться с `vault_client_v2`.
4. После подтверждения живой проверки — **C-3.3 «Мой план»**.

---

*C-3.3 и последующие этапы не реализовывались. Commit/push не выполнялись.*
