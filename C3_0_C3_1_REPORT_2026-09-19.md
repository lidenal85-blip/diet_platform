# ОТЧЁТ C-3.0 / C-3.1

> Дата: 2026-09-19 · Точка отката: git HEAD `a28b0f2` (tracked-дерево было чистым; `git checkout -- <файл>` возвращает любой файл).
> Границы этапа соблюдены: C-3.2 и далее не затронуты.

## 1. РЕЗУЛЬТАТ

**Выполнено.** C-3.0 — `meal_status_changed` добавлен в существующую таксономию (1 имя в frozenset, механизм/таблица/валидация не тронуты). C-3.1 — создан единый модуль `bot_handlers/keyboards.py`; все 6 старых определений главных клавиатур заменены compat-shim'ами на него; тексты кнопок, порядок, размеры и условия отображения сохранены **побайтово** (проверено assert-сверкой). Пользовательское поведение не изменилось.

## 2. ИЗМЕНЁННЫЕ ФАЙЛЫ

| Файл | Что изменено | Зачем | Почему в этом этапе |
|---|---|---|---|
| `planner/events.py` | +`meal_status_changed` в `PHASE0_EVENTS`; докстринг «7 имён» → «8 имён» с пометкой об одобрении | C-3.0: легальное имя для отметок блюд (сейчас их записать невозможно — `EventError`) | Ядро C-3.0; вызовы события — этап C-3.2, сюда не входят |
| `bot_handlers/keyboards.py` | **НОВЫЙ** — `MAIN_KEYBOARD_CANONICAL` (вариант bot.py, 9 кнопок), `MAIN_KEYBOARD_LEGACY` (вариант handlers, 10 кнопок), `canonical_main_kb()`, `legacy_main_kb()`, `main_kb()` | C-3.1: единый SoT клавиатур, устранение источника дрейфа меню | Ядро C-3.1 |
| `bot.py` | `MAIN_KEYBOARD = ReplyKeyboardMarkup(...)` → `MAIN_KEYBOARD = canonical_main_kb()` (имя и 9 кнопок идентичны) | Устранить дубликат №1 | C-3.1 |
| `bot_handlers/recipes.py` | Тело `main_kb()` → делегация в `keyboards.main_kb` (имя сохранено) | Устранить дубликат №2 (первоисточник легаси-варианта) | C-3.1 |
| `bot_handlers/cabinet.py`, `diet_picker.py`, `meal_schedule_v2.py`, `puhlyash_settings.py`, `schedule.py` | Тела `_main_kb()` → делегация в `keyboards.main_kb` (имя сохранено) | 5 модулей дублировали один и тот же делегат в recipes | C-3.1 |

Diff: **8 файлов, +21/−24** (7 из 8 — механические shim-замены; реальная логика только в events.py и новом keyboards.py).

## 3. C-3.0 — СОБЫТИЯ

- **Где находилась таксономия:** `planner/events.py:37` — `PHASE0_EVENTS` (frozenset, единственное место определения; реэкспорт через `planner/__init__.py`; других использований в кодовой базе нет — проверено grep). Запись — существующий `log_event()`; валидация имени + запрет чувствительных props (`FORBIDDEN_PROP_KEYS`) уже реализованы и **не изменялись**.
- **Как добавлено:** одно имя `meal_status_changed` в frozenset (после `plan_viewed`, перед `llm_call`). Событий фиксации действий с приёмами пищи ранее **не существовало** — meal_state хранит только `status`/`status_changed_at` (FACT аудита).
- **Что осталось без изменений:** таблица `events` (схема/индексы), механизм `log_event`, все 7 существующих имён и их вызовы, политика запретных props, решение о *местах вызова* нового события (C-3.2 — по указанию не добавлял ни одного вызова в handlers).

## 4. C-3.1 — КЛАВИАТУРЫ

- **Дублирования обнаружены (8 определений «главной», реально 2 уникальных):**
  1. `bot.py:34 MAIN_KEYBOARD` — 9 кнопок, **без** «🐈 Пухляш»;
  2. `bot_handlers/recipes.py:66 main_kb` — 10 кнопок, **с** «🐈 Пухляш»; остальные 5 (`cabinet`, `diet_picker`, `meal_schedule_v2`, `puhlyash_settings`, `schedule`) — одинаковые делегаты в recipes; 68 вызовов в сумме.
  - Прочие Reply/Inline-клавиатуры (цели FSM, меню расписаний, инлайн Пухляша) уникальны в своих модулях — не тронуты; `recipes_kb` не дублируется — не перенесён (не массовый рефакторинг).
- **Единый источник:** `bot_handlers/keyboards.py` — предсуществующего общего модуля клавиатур в проекте нет (проверено), второй модуль не создавался.
- **Что перенесено:** оба уникальных варианта — как есть; в модулях остались compat-shim с прежними именами (`_main_kb`/`main_kb`), чтобы не менять ни один из 68 вызовов (граница «избегай массового рефакторинга»).
- **Что осталось и почему:** сами shim-функции — удаление потребовало бы правки всех мест вызова (запрещено до подтверждения владельца). **Два варианта клавиатур сознательно НЕ объединены в одну:** объединение изменило бы видимые кнопки (9 против 10) — это пользовательское поведение, запрещённое этапом. Выбор одного варианта — решение этапа меню (C-3.5).

## 5. ПРОВЕРКИ

| Проверка | Команда/способ | Результат |
|---|---|---|
| Синтаксис всех 9 затронутых файлов | `python3 -m py_compile …` | **OK** |
| Импорты + циклы + идентичность текстов | python-скрипт: импорт всех 8 модулей + `bot`; assert-сверка всех строк кнопок обоих вариантов; сверка алиасов `bot.MAIN_KEYBOARD` / всех 6 shim'ов | **OK** (`KEYBOARD_TEXTS_IDENTICAL + ALL_ALIASES_OK`) |
| Таксономия | in-memory `log_event('meal_status_changed')` → OK; `made_up_event` → `EventError`; `len(PHASE0_EVENTS)==8` | **OK** |
| Регресс | `python3 -m pytest tests/ -q --ignore=tests/test_vault_integration.py` | **86 passed** (52.9s) — без изменений к baseline |
| Секреты в diff | `git diff \| gitleaks stdin` | **no leaks** |
| DB-выражения в diff | grep `CREATE TABLE\|ALTER\|INSERT INTO\|DROP` по `git diff` | **нет** |
| gitleaks-хук (pre-commit) | задействуется при следующем commit | не запускался (commit запрещён) |

**Не удалось проверить (и почему):**
- Импорт-проверка локально потребовала заглушку `apscheduler` (`schedule.py` импортирует `modules.scheduler.meal_scheduler`; пакета нет в Termux-окружении, на проде он есть). Проверка выполнена с минимальной заглушкой модуля; **зависимости в окружение не устанавливались**.
- Runtime-проверка на живом боте (отправка клавиатуры в Telegram) — запрещена этапом (нет рестарта/деплоя). Покрыта статической сверкой текстов и регрессом.
- `tests/test_vault_integration.py` исключён как и раньше (внешний `vault_client_v2` отсутствует локально) — предсуществующее ограничение, не связано с изменениями.

## 6. БЕЗОПАСНОСТЬ И ГРАНИЦЫ

- Схема БД не менялась: ✅ (0 DB-выражений в diff; таблицы/индексы/миграции не тронуты)
- Новые пользовательские сценарии не добавлялись: ✅ (тексты кнопок/условия идентичны — assert-сверка)
- Production-сервис не перезапускался: ✅
- Деплой не выполнялся: ✅
- Commit и push не выполнялись: ✅ (рабочее дерево содержит изменения; откат — `git checkout -- .` от HEAD `a28b0f2`)
- Чувствительные данные в события/логи не добавлялись: ✅ (`FORBIDDEN_PROP_KEYS` нетронут; props в новом событии не логировались ниоткуда — вызовов нет)

## 7. DIFF

```
 bot.py                            | 15 ++++-----------   ← MAIN_KEYBOARD → canonical_main_kb()
 bot_handlers/cabinet.py           |  3 ++-               ← shim
 bot_handlers/diet_picker.py       |  3 ++-               ← shim
 bot_handlers/meal_schedule_v2.py  |  3 ++-               ← shim
 bot_handlers/puhlyash_settings.py |  3 ++-               ← shim
 bot_handlers/recipes.py           | 10 +++-------        ← main_kb → shim
 bot_handlers/schedule.py          |  3 ++-               ← shim
 planner/events.py                 |  5 +++-               ← +meal_status_changed
 (new) bot_handlers/keyboards.py   | 67 +++++++++++++++    ← единый SoT
```
Затронутые файлы: 8 изменённых + 1 новый. Untracked вне этапа (не изменял, не коммитил): `C3_AUDIT_2026-09-19.md`, `promts/9.md`.

## 8. ОСТАВШИЕСЯ ПРОБЛЕМЫ

1. Два варианта главной клавиатуры (9 vs 10 кнопок) сосуществуют — объединение = пользовательское решение (C-3.5).
2. 6 compat-shim'ов — временное удобство; после решения №1 их можно свести к прямым импортам (мелкий рефакторинг, отдельное разрешение).
3. Runtime-поведение клавиатур на живом боте не проверялось (запрещено этапом) — проверить при первом деплое C-3.2+.
4. Вызова `meal_status_changed` пока нет нигде — так и задумано (C-3.2); до тех пор имя просто легально.

## 9. СЛЕДУЮЩИЙ ШАГ

**C-3.2 — экран «Сегодня»** (`bot_handlers/today.py`): handler «📅 Сегодня»/`/today` → `get_today_meals()` + inline-кнопки `ms:{id}:eat|skip|replace|recipe`, колбэки eat/skip пишут `set_meal_status` + `meal_status_changed`, отсутствию плана отдельное сообщение. По аудиту (§9) — до него при желании микро-шаг: добавление теста `test_meal_status_changed_taxonomy` в `test_planner_phase0.py` (сейчас таксономия покрыта косвенно).

## 10. ФИНАЛЬНЫЙ СТАТУС

**C-3.0 и C-3.1 завершены. Останов. К C-3.2 не перехожу без отдельного подтверждения владельца.**
