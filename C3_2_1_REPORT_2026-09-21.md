# ОТЧЁТ C-3.2.1 — Группировка действий экрана «Сегодня» по блюдам

> Дата: 2026-09-21 · Основание: задача владельца C-3.2.1 · Входит в C-3 (Reader-first)
> Статус: **ВЫПОЛНЕНО** · Останов после отчёта — C-3.3 не начинался

---

## 1. РЕЗУЛЬТАТ

**Выполнено.** Каждое блюдо экрана «Сегодня» теперь имеет собственный визуальный блок: ряд-заголовок (приём · блюдо · статус) и непосредственно под ним — его два ряда действий. Нажатие любой кнопки влияет только на запись meal_state этого блюда. Логика переходов, идемпотентность, принадлежность записей и события не менялись.

## 2. ИЗМЕНЁННЫЕ ФАЙЛЫ

| Файл | Что изменено | Зачем |
|---|---|---|
| `bot_handlers/today.py` | 1) Новая кнопка-заголовок блока блюда (`_meal_header_text`, callback `today:meal:{id}`, хендлер `today_meal_info` с инфо-алертом); 2) `_today_kb_with_refresh` строит клавиатуру блоками: на каждый приём 3 ряда (заголовок + 2 ряда действий), в конце «🔄 Обновить» | Требование C-3.2.1: кнопки визуально привязаны к блюду |
| `tests/test_today.py` | Обновлён тест структуры клавиатуры (3 ряда × N приёмов + 1); добавлены 2 новых теста (`test_each_meal_has_own_action_block`, `test_meal_header_info_alert_and_guards`); усилен `test_refresh_redraws` (статус ✅ виден в заголовке блока) | Покрытие пунктов 1, 2, 7 ТЗ C-3.2.1 |

Других изменений нет. Файлы `bot.py`, `keyboards.py`, planner/ и прочие не трогались (их модификации в `git status` — незакоммиченные C-3.0–C-3.2, датируемые прежними этапами).

## 3. КАК ТЕПЕРЬ ФОРМИРУЕТСЯ КЛАВИАТУРА

**Было (C-3.2):** один список блюд в тексте + одна клавиатура внизу: N×2 ряда действий вперемешку + «Обновить». Привязка «кнопка → блюдо» — только по порядку.

**Стало (C-3.2.1):** на каждый приём пищи — блок из 3 рядов:

```
Завтрак — Овсянка на молоке с бананом        ← текст экрана
┌─────────────────────────────────────────┐
│ 🍳 Завтрак · Овсянка на молоке с бананом │  ← ряд-заголовок (инфо-алерт)
│ [✅ Съел] [⏭ Пропустил]                  │  ← действия этого блюда
│ [🔁 Заменить] [📖 Рецепт]                │  ← действия этого блюда
└─────────────────────────────────────────┘
… следующий блок (Обед) …
[🔄 Обновить]
```

Детали:

- **Заголовок блока** — кнопка `text="{Приём}{значок статуса} · {блюдо}"`, callback `today:meal:{meal_state_id}` (только UUID, без персональных данных). Нажатие → инфо-алерт: приём, полное название блюда, человекочитаемый статус («запланировано» / «съедено ✅» / «пропущено ⏭» / «заменено 🔁»). Ничего не меняет.
- **Название блюда в заголовке** обрезается до 30 символов («…») — кнопка остаётся компактной на мобильном; полное название всегда в тексте экрана и в алерте.
- **Callback действий** не изменились: `ms:{id}:eat|skip|replace|recipe` — формат Telegram (до 64 байт) соблюдён, UUID ~36 символов.
- **Отображение статуса**: значок уже был в тексте экрана (✅/⏭/🔁/💤), теперь дополнительно дублируется в заголовке блока.
- **Ограничение Telegram**: inline-кнопки нельзя вставить между строками текста одного сообщения, поэтому «кнопки непосредственно под блюдом» реализованы секционным заголовком — канонический паттерн для одного сообщения с перерисовкой. Альтернатива (отдельное сообщение на блюдо) сломала бы атомарную перерисовку экрана и создала бы спам — сознательно не выбрана.

## 4. СОХРАНЕНО (подтверждение по коду и тестам)

- Источник данных: только `planner.get_today_meals()` (canonical chain) — не тронут.
- Переходы статусов: guard `planned → eaten|skipped` в `_handle_status` — не тронут.
- Идемпотентность повторных нажатий — не тронута (тест `test_repress_is_noop` зелёный).
- Принадлежность записи пользователю — не тронута (тесты чужой записи зелёные; хендлер заголовка использует ту же проверку).
- События: формат и минимальный состав props без изменений; заголовок блоков не пишет никаких событий (проверено тестом).
- Заглушки «Заменить» (честное «позже») и «Рецепт» (только сохранённый рецепт) — не тронуты (тесты не генерации зелёные).
- Главное меню, `/today`, «🔄 Обновить», пустое состояние — без изменений (тесты зелёные).
- Схема БД — не менялась (0 DDL в diff, проверено грепом добавленных строк).

## 5. ТЕСТЫ И ПРОВЕРКИ

| Проверка | Результат |
|---|---|
| `pytest tests/test_today.py -q` | **18 passed** (16 прежних + 2 новых) |
| `pytest tests/ -q` (полная регрессия) | **107 passed, 10 failed** — все 10 фейлы `test_vault_integration.py`, предсуществующие (импортируют `vault_client_v2`, которого нет ни локально, ни на сервере; проверено: `FAILED \| grep -v vault` → 0) |
| `py_compile` today.py, test_today.py | OK |
| DDL-скан добавленных строк diff | нет (схема не менялась) |
| Smoke на реальной temp-БД (init_db → plan → 21 meal_state → `_today_kb_with_refresh`) | OK: 21 запись, 64 ряда, структура блоков верная |
| Коллизии callback-префиксов | нет: `today:meal:` уникален, занятые префиксы (`ph:`, `m:*`, `verify:`, `reason:`, `react:`, `diet:`, `pending_diet:`, `ms:`, `today:refresh`) не пересекаются |

Соответствие пунктам ТЗ §ТЕСТЫ: 1 ✅ (блоки, тест 17), 2 ✅ (callback содержит id, тест 17), 3 ✅ (`test_press_eat`), 4 ✅ (`test_press_skip`), 5 ✅ (`test_repress_is_noop`, `test_repress_no_second_event`), 6 ✅ (`test_foreign_meal_rejected`), 7 ✅ (`test_refresh_redraws` + значки в заголовках), 8 ✅ (`test_empty_*`, `test_main_menu_*` — без изменений, зелёные).

## 6. ОГРАНИЧЕНИЯ И ОСТАВШИЕСЯ ПРОБЛЕМЫ

1. **Деплой на whimco не выполнялся** — сервер всё ещё работает на версии C-3.2 (одна общая клавиатура). Обновление экрана «Сегодня» до C-3.2.1 — отдельный шаг по авторизации владельца.
2. Если у пользователя приёмов за день > 6, клавиатура станет высокой (19+ рядов) — Telegram это допускает, но UX стоит проверить на проде и при необходимости ввести пагинацию/сворачивание (вне C-3.2.1).
3. Незакрытые хвосты прежних этапов (не относятся к C-3.2.1): `RECIPES_SCHEMA` не исполняется `init_db()`; хендлер «Сегодня» без `StateFilter`; vault-тесты непроверяемы в обоих окружениях.
4. Импорт `bot_handlers.today` вне pytest-окружения тянет цепочку с apscheduler (нет на телефоне) — известная особенность локали, на проде модуль есть; тесты это обходят.

## 7. ГРАНИЦЫ (подтверждение)

- Схема БД не менялась, новые таблицы/поля не добавлялись ✅
- Рецепты/замены не генерируются, новые LLM-вызовы не добавлены ✅
- Другие разделы бота не менялись ✅
- **Commit, push, deploy не выполнялись** ✅ — все изменения в рабочем дереве; точка отката: `git checkout -- bot_handlers/today.py tests/test_today.py` (приводит к состоянию C-3.2) либо полный откат на HEAD `a28b0f2`.

## 8. СЛЕДУЮЩИЙ ШАГ (предложение, не выполняется)

Деплой C-3.2.1 на whimco (2 файла, md5-сверка, рестарт по той же схеме с бэкапом) и живая проверка экрана «Сегодня» в Telegram — после этого логично закоммитить накопленное C-3.0–C-3.2.1 и переходить к C-3.3 «Мой план».

---

**C-3.2.1 COMPLETE — ОЖИДАНИЕ РЕШЕНИЯ ВЛАДЕЛЬЦА**
